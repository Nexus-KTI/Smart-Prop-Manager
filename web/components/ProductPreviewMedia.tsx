import { MarketingDashboardMock } from "@/components/MarketingDashboardMock";

/**
 * Full product chrome preview for “What’s live today”.
 * In-page mock (Nexora + shared formatNaira) — not a live logged-in session.
 */
export function ProductPreviewMedia() {
  return <MarketingDashboardMock variant="full" />;
}
